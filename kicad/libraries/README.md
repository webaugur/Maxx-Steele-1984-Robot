# cop41xl KiCad library

Vendored from [github.com/webaugur/cop41xl-kicad-library](https://github.com/webaugur/cop41xl-kicad-library) — COP400-series schematic symbols by David L Norris.

## Update

```bash
cd kicad/libraries
# git pull   # if tracking upstream as submodule clone
```

Both [`transmitter/`](../transmitter/) and [`receiver/`](../receiver/) reference this directory via `${KIPRJMOD}/../libraries/`.

Symbol used on the remote MCU board: `cop41xl:COP411L` (20-pin DIP).