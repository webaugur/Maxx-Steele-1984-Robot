#!/usr/bin/env bash
# Cartridge ROM utilities: disasm, validate, template, opcodes.
set -euo pipefail
BIN_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TOOLS_DIR="$(cd "$BIN_DIR/.." && pwd)"
exec python3 "$TOOLS_DIR/maxx_rom.py" "$@"