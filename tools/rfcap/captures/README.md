# IQ capture data (local only)

GNU Radio `.dat` IQ recordings live here. Large files are **gitignored** — see the [file catalog](../README.md#iq-capture-files-local-only).

New recordings from `RemoteSpectrum.grc` / `RemoteSpectrum.py` are written to this folder automatically.